// Guessed/unguessed are universal right/wrong signals, not brand colors — kept as fixed
// semantic hex (same precedent as the '#ff6b6b' inline-error color elsewhere) rather than
// theme tokens, so they read the same in every theme. Shared by GuessGame and StatsTable so a
// player is colored identically in both views.
export const GREEN = '#22c55e'
export const RED = '#ef4444'
